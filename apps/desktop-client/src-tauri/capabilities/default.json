{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow the local main window to invoke only application-owned commands; no Tauri core or plugin permissions are required.",
  "local": true,
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": []
}
