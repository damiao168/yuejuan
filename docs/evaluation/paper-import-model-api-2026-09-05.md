# 考试资料识别与学校模型配置验收（2026-09-05）

## 本次完成

- 识别轮询保持页面内容，短暂请求失败后继续查询；修复并发查询导致加载状态不结束的问题。
- 停止识别后保留上传资料，支持重新识别。新一轮识别清理上一轮文字及结构化提示，处理中不展示旧结果告警。
- 无考试题目、答案、解析或评分标准时提示“未识别到考试内容”。
- 平台模型配置按学校隔离；快速切换学校忽略过期响应，保存期间锁定学校和抽屉，表单校验失败不产生未处理异常。
- 学校默认 API 实际用于考试资料解析。配置由网关按当前租户选择，只通过已鉴权的内部解析请求传递，不覆盖全局模型。没有默认配置时使用本地模型；停用的默认配置不会静默回退。
- 支持 OpenAI 兼容 JSON 模式及 DashScope 原生协议，并继续执行输出结构校验。外部连接使用公共 HTTPS 地址、固定解析后的目标 IP、验证证书且不跟随重定向。

## 验证证据

- AI 服务完整测试：113 项通过，45 个子测试通过。
- Go：`internal/modelgovernance`、`internal/paper`、`internal/server`、`internal/auth` 通过；已有文件下载相关 `internal/files` 测试通过。
- 管理端 TypeScript 检查、生产构建、修改页面的 Biome 检查通过。
- 浏览器回归：轮询短暂失败后恢复、滚动位置不变、停止与重试、无关内容提示、重试隐藏旧告警、学校切换过期响应隔离、配置校验共两个场景通过。
- 原对话中的无关图片任务在本地部署上实际完成“重新识别 → 停止 → 重新识别 → 等待核对”：上传资料始终保留，最终无题目，包含 `NO_EXAM_CONTENT_DETECTED`，`error_code` 为空。
- 本地 Qwen 模型已恢复，AI 服务 `/ready` 返回 `ready`。

## 验证范围

当前数据库没有第三方 API 配置，因此没有使用真实供应商密钥做付费调用验收。按学校选取配置、请求协议、输出校验、密钥不进入提示词、内部请求传递均使用合成数据验证。实际供应商是否支持指定模型及 JSON 模式，需填入该校配置后通过连接测试并试解析确认。

进度百分比是阶段指示，不是逐字识别的精确完成比例。第三方 API 用于考试资料解析，主观题评分仍遵循原模型治理和教师复核流程。
