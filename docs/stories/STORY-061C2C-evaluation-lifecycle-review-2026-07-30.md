# STORY-061C2C 评测生命周期操作实现复审

日期：2026-07-30

## 交付结论

评测中心已形成“创建批次—录入候选—冻结完成—标记失效”的可见操作闭环，并直接调用 STORY-061C1 已有的严格管理 API。

## 生命周期约束

- 所有写操作仅对具备 `model:evaluation:manage` 的用户显示。
- 协议样例不允许录入教师复核或接受数据；授权冻结集强制全部样本经过教师复核。
- 候选固定绑定 Deployment、模型版本、提示词版本、Rubric 版本和批次样本规模。
- 冻结完成前，界面要求至少两个候选且包含本地基线；服务端继续作为最终门禁。
- 完成后不再添加候选；数据授权撤回、版本不一致或证据被替代时可标记失效。
- 每次创建、录入和状态转换均要求填写原因，由后台写入审计。

## 明确未做

- 没有 promotion/revocation。
- 没有外部厂商网络调用。
- 没有启用模型部署或修改租户路由。
- 没有让模型建议写入最终成绩。

## 自动门禁

新增 `npm run check:story061`，静态确认：

- Web API 与 Go 路由保持一致。
- 页面展示完整的质量、证据和成本指标。
- 生命周期操作具有权限与状态门禁。
- 完成条件明确包含本地基线。
- STORY-061C2 不出现 promotion 实现。

