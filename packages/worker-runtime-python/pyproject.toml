[project]
name = "edugrade-worker-runtime"
version = "0.1.0"
description = "Shared lease protocol for EduGrade Python workers"
requires-python = ">=3.11,<3.13"
dependencies = []

[build-system]
requires = ["setuptools==83.0.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["edugrade_worker_runtime*"]
